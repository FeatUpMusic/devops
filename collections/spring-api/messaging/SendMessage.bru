meta {
  name: Send Message
  type: http
  seq: 1
}

post {
  url: {{baseUrl}}/api/messages
  body: json
  auth: bearer
}

headers {
  Authorization: Bearer {{token_A}}
}

body:json {
  {
    "recipientId": {{userB_id}},
    "text": "Hey Bob! Let's collab on that track!"
  }
}
